# Copy before deploying the optional auth-only API (community-priorities-auth-api).
# Production maps at https://d1b6znwb7yuvt4.cloudfront.net currently use the existing
# dev API for login: https://tfqmwiadc8.execute-api.us-east-1.amazonaws.com
JWT_SECRET=
JWT_EXPIRES_IN=24h

# Optional overrides for default map login users (see src/auth/users.ts)
# CEA_ADMIN_PASSWORD=
# CFM_ADMIN_PASSWORD=
# CLUSTERS_ADMIN_PASSWORD=
# SUPER_ADMIN_PASSWORD=
